# GTOHJS Git 镜像开发入口

这是可单独下载的 GTOHJS 发布源码镜像。它不依赖本机活动源码、缓存或其他工作区目录；不得把本机绝对路径写入提交内容。

本镜像中的机器、配方和 API 代码以镜像内源码及 `../docs` 为准。修改配方时必须保持 GT 原生配方窗口约束；先阅读镜像内相关文档。

Java 工具链速查见 `JAVA_TOOLCHAIN_ZH_EN.md`；GTOLib 使用 JDK 23 toolchain 但目标 Java 21，GTOHJS/API 默认使用 JDK 21。
